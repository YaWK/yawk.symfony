{# templates/user_list.html.twig #}

{% extends 'base.html.twig' %}

{% block body %}
    <h1>User List</h1>

    <ul>
        {% for user in users %}
            <li>
                {{ user.username }}
                {# Add other properties you want to display here. #}
            </li>
        {% endfor %}
    </ul>
{% endblock %}
